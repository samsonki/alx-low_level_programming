#include "main.h"

/**
 * _abs - print absolute value
 * @n: return integer
 * Description: print_absolute value.
 * Return: absolute value.
 */
int _abs(int n)
{
	if (n < 0)
		return (n * -1);
	return (n);
}
